name: SSH server
on:
  watch:
    types: started
  repository_dispatch:
  workflow_dispatch:

jobs:
  Ubuntu:
    runs-on: ubuntu-latest
    steps:
      - name: SSH connection to Actions
        uses: P3TERX/debugger-action@master
